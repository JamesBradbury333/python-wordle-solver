[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[scripts]
format = "black ."
lint = "flake8 -v"
unit-test = "python -m pytest tests/unit -v"

[packages]
scipy = "*"
pytest = "*"

[dev-packages]

[requires]
python_version = "3.10"
